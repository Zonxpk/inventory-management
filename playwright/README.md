# Playwright test coverage

This folder contains Playwright API and UI tests mapped from ASSIGNMENTS.MD functional requirements.

## Run

- Install browsers: `npx playwright install chromium`
- Run all: `npm run test:playwright`
- API only: `npm run test:playwright:api`
- UI only: `npm run test:playwright:ui`
